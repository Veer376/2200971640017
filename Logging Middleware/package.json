{
  "name": "logging-middleware",
  "version": "1.0.0",
  "description": "A reusable logging middleware for both backend and frontend applications",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "logging",
    "middleware"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.10.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  }
}
